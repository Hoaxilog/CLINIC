<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ $reportTitle }}</title>
    <style>
        :root { color-scheme: light; }
        * { box-sizing: border-box; }
        body {
            margin: 0;
            font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
            color: #0f172a;
            background: #ffffff;
            font-size: 12px;
            line-height: 1.45;
        }
        .page {
            max-width: 1120px;
            margin: 0 auto;
            padding: 24px;
        }
        .header {
            border-bottom: 2px solid #0f766e;
            padding-bottom: 12px;
            margin-bottom: 12px;
        }
        .clinic { margin: 0; font-size: 20px; font-weight: 700; color: #0f766e; }
        .system { margin: 2px 0 10px; font-size: 12px; color: #334155; }
        .meta { margin: 2px 0; color: #334155; }
        table {
            width: 100%;
            border-collapse: collapse;
            border: 1px solid #334155;
            margin-top: 8px;
        }
        th, td {
            border: 1px solid #64748b;
            padding: 7px 8px;
            text-align: left;
            vertical-align: top;
        }
        th {
            background: #f8fafc;
            font-size: 10px;
            text-transform: uppercase;
            letter-spacing: .04em;
        }
        .empty { text-align: center; color: #64748b; padding: 14px; }
        .footer {
            margin-top: 18px;
            border-top: 1px solid #cbd5e1;
            padding-top: 8px;
            color: #475569;
            display: flex;
            justify-content: space-between;
            gap: 12px;
            flex-wrap: wrap;
        }
        .signature {
            margin-top: 22px;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 24px;
        }
        .line {
            border-top: 1px solid #64748b;
            padding-top: 6px;
            color: #475569;
        }
        @media print {
            .page { max-width: none; padding: 0; }
            @page { size: auto; margin: 12mm; }
        }
        @media (max-width: 768px) {
            .signature { grid-template-columns: 1fr; }
        }
    </style>
</head>
<body>
    <main class="page">
        <header class="header">
            <p class="clinic">Tejada Clinic</p>
            <p class="system">Tejada Clinic</p>
            <p class="meta"><strong>Report:</strong> {{ $reportTitle }}</p>
            <p class="meta"><strong>Generated By:</strong> {{ $generatedBy }}</p>
            <p class="meta"><strong>Date Generated:</strong> {{ $generatedAt->format('M d, Y h:i A') }}</p>
            <p class="meta"><strong>Date Range:</strong> {{ \Carbon\Carbon::parse($fromDate)->format('M d, Y') }} - {{ \Carbon\Carbon::parse($toDate)->format('M d, Y') }} ({{ $rangeLabel }})</p>
        </header>

        <section>
            @if ($reportType === 'appointments')
                @php
                    $totalRecords = $rows->count();
                    $totalPaymentMade = (float) ($rows->sum('payment_made') ?? 0);
                    $totalTreatmentCost = (float) ($rows->sum('treatment_cost') ?? 0);
                    $netTotal = $totalPaymentMade - $totalTreatmentCost;
                @endphp
                <p class="meta"><strong>Findings:</strong> {{ number_format($totalRecords) }} records, payment made PHP {{ number_format($totalPaymentMade, 2) }}, treatment cost PHP {{ number_format($totalTreatmentCost, 2) }}, net PHP {{ number_format($netTotal, 2) }}.</p>
            @endif
            <table>
                <thead>
                    <tr>
                        @foreach ($columns as $column)
                            <th>{{ $column }}</th>
                        @endforeach
                    </tr>
                </thead>
                <tbody>
                    @forelse ($rows as $row)
                        @if ($reportType === 'patients')
                            <tr>
                                <td>{{ $row->id }}</td>
                                <td>{{ trim(($row->last_name ?? '') . ', ' . ($row->first_name ?? '') . ' ' . ($row->middle_name ?? '')) }}</td>
                                <td>{{ $row->gender ?? 'Unspecified' }}</td>
                                <td>{{ $row->birth_date ? \Carbon\Carbon::parse($row->birth_date)->format('M d, Y') : '-' }}</td>
                                <td>{{ $row->mobile_number ?? '-' }}</td>
                                <td>{{ $row->created_at ? \Carbon\Carbon::parse($row->created_at)->format('M d, Y h:i A') : '-' }}</td>
                            </tr>
                        @elseif ($reportType === 'monthly-summary')
                            <tr>
                                <td>{{ $row->metric ?? '-' }}</td>
                                <td>{{ $row->value ?? '-' }}</td>
                            </tr>
                        @else
                            <tr>
                                <td>{{ $row->id }}</td>
                                <td>{{ trim(($row->last_name ?? '') . ', ' . ($row->first_name ?? '') . ' ' . ($row->middle_name ?? '')) }}</td>
                                <td>{{ $row->service_name ?? 'N/A' }}</td>
                                <td>{{ $row->treatment_performed ?: 'N/A' }}</td>
                                <td>{{ $row->appointment_date ? \Carbon\Carbon::parse($row->appointment_date)->format('M d, Y h:i A') : '-' }}</td>
                                <td>{{ $row->status ?? '-' }}</td>
                                <td>PHP {{ number_format((float) ($row->payment_made ?? 0), 2) }}</td>
                            </tr>
                        @endif
                    @empty
                        <tr>
                            <td colspan="{{ count($columns) }}" class="empty">No records found for the selected criteria.</td>
                        </tr>
                    @endforelse
                </tbody>
            </table>
        </section>

        <footer class="footer">
            <div>Generated by Tejada Clinic</div>
            <div>Page 1 of 1</div>
        </footer>

        <section class="signature">
            <div class="line">Prepared by</div>
            <div class="line">Approved by</div>
        </section>
    </main>

    <script>
        window.addEventListener('load', () => window.print());
    </script>
</body>
</html>
